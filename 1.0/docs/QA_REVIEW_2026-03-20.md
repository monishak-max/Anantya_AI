# QA review for prepared v1.4 package

## What was checked
- Python syntax compilation across the package
- prompt pack structure and file presence
- weekly/monthly schema alignment
- package hygiene cleanup
- static inspection of the updated bridge, schema, pipeline, style guard, and web routing

## Result
- Static syntax check passed for the updated Python files.
- `PeriodOverviewInput` is now supplied with both `period_start` and `period_end`.
- Union deep read now receives a genuinely deeper input mode than union snapshot.
- Chart reveal specificity guard is no longer effectively disabled.
- The package no longer ships `.env`, `.cache`, `__pycache__`, or the stale v1.3 PDF report.

## Important limitation
This environment does not include the full DE440 runtime assets used by the calculator layer, so end-to-end live chart generation was not executed here. The QA is therefore static and structural, not live-generation QA.

## Recommended next team step
Run a live QA pass on these surfaces using the actual runtime:
- chart reveal
- now collapsed
- now expanded
- birth chart core
- union deep read
- weekly overview
- monthly overview

Focus the live review on:
- specificity
- yoga handling discipline
- dasha prioritization
- remedy/modifier restraint
- tone consistency with Anantya's persona

Additional patch
- Added present-centered birth-chart framing in prompt + bridge layers. Static QA only; live generations should confirm the prose now starts from present tense and uses past/future as arc rather than as the main frame.
